import React, { Component } from 'react';

export const Note = ({children}) => 
    (<div className="jumbotron">
                <div>
                    {children}
                </div>
    </div>
    )
